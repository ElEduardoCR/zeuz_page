flask>=3.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Pasarelas
mercadopago>=2.2.0
stripe>=11.0.0

# Persistencia. psycopg2 solo se usa cuando DATABASE_URL apunta a Postgres;
# en local con SQLite queda instalado pero inactivo.
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0

gunicorn>=22.0.0
